# 현재 상태

- 2026-09-20: 사용자 요청으로 3주차 과제·실행증빙을 GitHub `jeonyoungjun1233/AI-MLQps`의 main에 커밋 `0f83adc`로 푸시하고 원격 커밋 일치를 확인했다. 추가 파일의 주요 인증정보 패턴 검사를 통과했고, `.gitattributes`로 줄바꿈 변환을 방지하여 Git blob의 코드·입력·제출 JSON SHA-256이 실행증빙과 일치함을 확인했다. LMS 제출은 미수행이다.

- 2026-09-20: 사용자 요청에 따라 `03주차_강의자료.pdf` 전체 10쪽을 읽고 별도 `3주차 과제 완성본` 폴더를 만들었다(이번 사용자 지정 위치). 교수자 원본 Compose·실습 3.1 코드·requirements를 복사해 실제 실행했고 원본과 SHA-256 일치를 검증했다. 서비스 6개, 호스트 포트 7개, JSON 의존성 2건, 원본 Compose 의존성 3건, 보안 경고 2건이다. 맵 형태 `kafka → zookeeper (service_healthy)`가 리스트만 읽는 원본 파서에서 빠진 원인을 최종 설계표에 반영했으며 제출 JSON은 수정하지 않았다.
- 3주차 제출 JSON: `3주차 과제 완성본/practice/chapter3/data/output/ch3_compose_plan.json`, generated_at=`2026-09-20T02:27:06+00:00`, SHA-256=`120dce5709c27693ac8035212750122c26d3bc9e9eaafa5fa2164419fe768441`. PDF 2쪽·텍스트 대체본·LMS용 답안·제출 안내와 실행증빙을 구성했다. 사전 설계 및 서비스/포트/의존성 예측 기록은 사용자에게 질문했으나 제공된 기록이 없어 미기록으로 명시했다. 사전 설계 활동 자체를 완료한 것으로 보지 않는다.
- 재현 명령: `python "3주차 과제 완성본/실행증빙/재실행.py"`. 별도 경로에서 원본 코드를 실행해 정적 값 일치와 제출 JSON 불변을 검증했다. Docker 컨테이너 기동·외부 API 호출은 하지 않은 구성 정적 분석이다. 교수자 저장소의 기존 `.env.example` 변경은 건드리지 않았다. 3주차 GitHub 푸시는 완료했고 LMS 제출 및 실제 수업일/마감 시각은 미확인이다.

- 최종 실시간 제출물 커밋 `3233e4f0ef3a4865f3cb2eae3115a3b0ca147731`을 지정 GitHub 저장소 main에 푸시하고 원격 커밋 일치를 확인했다. 추적 파일 61개에서 사용자 제공 인증값(인코딩/디코딩 형태 포함)과 주요 인증키 패턴이 검출되지 않았고, Git에 저장된 원본·입력·산출물 해시가 검증결과와 일치했다.

- 2026-09-13 22:24 KST: 사용자가 채팅으로 제공한 키를 메모리/프로세스 환경변수에서만 사용해 에어코리아 API 호출 성공(HTTP 200, resultCode=00). 2026-09-13 22:00 서울 40개 측정소 응답을 `01주차 과제 제출/02주차/practice/chapter2/data/input/airquality_seoul_live.json`에 원문 그대로 저장했다. 키는 새 파일이나 Git에 기록하지 않았다.
- 최종 02주차 제출물은 위 실시간 응답의 EDA와 교수자 기준 2026-06-28 22:00 대비 KS 검정이다. 간격 77일, baseline_n=current_n=40, PM10 평균 20.025→42.35, KS=1.0, p_value=1.860340365603627e-23, alpha=0.05, drift_detected=true. 두 JSON·답안(2문장/3문장)·로그·응답 및 코드 해시 검증을 완료했다. 기본 재실행은 `실행설정.json`의 최종 실시간 저장 응답을 사용하며 새 API 호출은 하지 않는다.
- 법령 API 실제 호출은 HTTP 200이지만 본문에 사용자 정보 검증 실패 및 서버 IP주소·도메인주소 등록 요구가 반환됐다. 법령 API 정상 검색은 미완료다. 02주차 필수 과제는 에어코리아만 사용하므로 제출물 준비는 완료했다. LMS 업로드는 미수행이다.

## 이전 실행 이력 (최종 제출값과 구분)

- 2026-09-13 재확인: 사용자가 `.env.example` 위치를 지정하고 저장 완료를 알린 뒤에도 실제 디스크의 `02_강의저장소/mlops_public/.env.example` 12행·15행은 각각 `YOUR_DATA_GO_KR_API_KEY`, `YOUR_LAW_OPEN_API_OC`였다(파일 수정 시각 2026-09-06 11:26:04). 사용자 홈에서 발견한 다른 `.env.example` 2개에는 해당 변수명이 없었다. 편집기에서 저장한 파일의 전체 경로를 질문한 상태이며, 이번에도 실시간 요청은 보내지 않았다. 기존 스냅샷 제출물은 재실행 검증을 통과했고, 2번 답안을 false인 경우의 해석임이 명확하도록 다듬었다. 실제 키 활용 완료나 LMS 제출 완료로 보고하지 않는다.

- 2026-09-13 22:11 KST: 02주차 PDF 전체 10쪽과 9. 과제를 확인하고 `01주차 과제 제출/02주차`에서 교수자 원본 실습 2.1·2.2를 새로 실행·검증했다. JSON 2개는 `practice/chapter2/data/output/`에, 서술형 답안은 `02주차_서술형_답안.txt`에 보관했다(2문장·3문장). p_value=0.5786001416508443, alpha=0.05, baseline_n=current_n=40, drift_detected=false. PM10 평균 20.025→20.775, KS=0.175. 원본 해시·실행 로그·입력·버전·재실행 스크립트·검증결과.json을 실행증빙에 보관했다.
- 이번 02주차 실행은 교수자 제공 실제 응답 스냅샷(2026-06-28 22:00→23:00)의 재계산이다. 현재 `.env.example`에는 두 API 인증 설정 모두 안내용 값이며 다른 로컬 `.env`와 프로세스·Windows 사용자/시스템 환경변수에서도 실제 키를 찾지 못했다. 이번 점검의 API 요청 횟수는 0이다(`request_sent=false`). 사용자에게 실제 키 저장 위치를 질문했으며 실시간 연결 성공은 아직 확인하지 못했다. 과거 아래의 1주차 HTTP 403 기록과 구분한다.
- 02주차 산출물 커밋 `fa2d19254458e6cda28804e22d8986c1e771b901`을 지정 GitHub 저장소의 main에 푸시했고 `git ls-remote`로 일치를 확인했다. 추적 파일 59개에서 주요 인증키 패턴 검사를 통과했고, Git에 저장된 원본·입력·JSON·로그의 SHA-256도 실행증빙과 일치했다. 교수자 저장소 작업 트리는 변경 없이 깨끗하다. 02주차 LMS 업로드와 제출 완료 상태는 미확인이다.

- 2026-09-13 12:17 KST: 사용자 지정 `02_강의저장소/mlops_public/.env.example`의 DATA_GO_KR_API_KEY를 메모리에서 읽어 서울 실시간 API를 2회 호출했다. 모두 HTTP 403, 오류 30 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` (등록되지 않은 서비스키). 발급/승인 시각을 몰라 동기화 지연인지 키 문제인지 확정할 수 없다. 키 값은 새 파일에 기록하지 않았고, 기존 스냅샷 제출물은 변경하지 않았다. 증빙: `01주차 과제 제출/실행증빙/API키_호출확인.json`.

- 과목: AI 데이터개발과 MLOps. 전영준 / 202373020.
- 사용자 요청으로 기존 00_제출할파일_1주차, 03_1주차과제의 제출물과 코드를 삭제하고 `01주차 과제 제출`에 새로 구성했다.
- API 키 없이 제공된 실제 응답 스냅샷을 새로 실행했다. 원본 측정 시각 2026-06-28 22:00, 측정소 40, PM10 보고 40, 결측 0, 평균 20.0, PM2.5 평균 12.7. source=snapshot.
- 입력 원본·교수자 코드·재실행 코드·로그·검증 기록은 새 폴더의 실행증빙에 모았다. 외부 저장소 없이도 표준 라이브러리로 재실행된다.
- 답안 제목은 `01주차 과제 제출`. PDF 1쪽, 세 문항 각각 세 문장. PDF/TXT/JSON과 선택용 ZIP을 같은 폴더에 준비했다.
- 사전 예측을 하지 않았다는 기존 사용자 답변에 따라 1번은 미작성 사실과 사후 관찰을 기록했다. 사전 예측 활동은 미충족이다.
- 출처: 1주차 강의 PDF 9쪽 스냅샷 실행 안내, 10-11쪽 과제 조건. 강의계획서는 수업일 포함 7일 이내 LMS 제출을 안내한다.
- LMS 마감 시각과 허용 확장자는 확인되지 않았으며 실제 LMS 제출은 미수행이다.
- 공개 GitHub 저장소: https://github.com/jeonyoungjun1233/AI-MLQps, main. 기존 이력을 유지하고 새 제출물을 반영한다.
- 교수자 원본 저장소: 02_강의저장소/mlops_public, commit 9b6618ca6efa5ca22f45301b824e62140519c204. 개인 제출물 재작성으로 원본은 수정하지 않았다.
- 02_강의PDF에 주차별 13개·119쪽 통합본이 있으며 전체 강의 목차는 PDF로 바로 연결된다.
